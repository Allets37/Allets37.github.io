{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tailwind",
            "type": "shell",
            "command": "npx tailwindcss -i custom.css --output tailwind.css --watch",
            "runOptions": {
                "runOn":"folderOpen"

            },
            "presentation": {
              "reveal": "never",
              "revealProblems": "onProblem",
              "panel": "dedicated"
            }
        }
    ]
}